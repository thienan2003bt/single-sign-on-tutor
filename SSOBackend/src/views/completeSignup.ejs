<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <!-- JQuery -->
    <script src="
    https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js
    "></script>


    <title>Single Sign On Tutorial</title>
</head>

<body>
    <input id="ssoToken-input" value="<%= ssoToken %>" hidden />
    <h3>We are redirecting you to the original application, please wait ...</h3>

</body>

<script>
    const redirectURL = localStorage.getItem('serviceURL');
    const ssoToken = $('#ssoToken-input').val();
    localStorage.removeItem('serviceURL');
    $(document).ready(() => {
        window.location.replace(`${redirectURL}/code?ssoToken=${ssoToken}`);
    })
</script>

</html>